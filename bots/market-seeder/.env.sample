# Core endpoints
BASE_URL="http://localhost:8000"
WS_URL="ws://localhost:8003"
DATABASE_POSTGRES_URL="postgres://admin:mypass@localhost:5432/postgres?sslmode=disable"

# Dummy users
PASSWORD="bot-password"
USER_COUNT="20"
WALLET_BALANCE="500000000" # cents
ASSET_QTY="10000"

# Orders
PRICE_MIN="50"  # dollars
PRICE_MAX="500" # dollars
QTY_MIN="1"
QTY_MAX="5"
ORDERS_PER_MARKET_MIN="900"
ORDERS_PER_MARKET_MAX="1100"
PAIR_RATIO="0.6" # fraction of orders sent as matched BUY/SELL pairs
ORDER_JITTER_MS="10"

# Optional limits
MARKET_LIMIT=""
WS_MODE="per-market" # per-market | per-user
REPORT_DIR="reports"
